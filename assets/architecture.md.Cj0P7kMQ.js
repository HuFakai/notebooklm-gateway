import{_ as s,o as e,c as i,a2 as t}from"./chunks/framework.DDPo1y9X.js";const k=JSON.parse('{"title":"架构决策：稳定版公开 API + 自有薄网关","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md","lastUpdated":1783971185000}'),n={name:"architecture.md"};function l(o,a,p,d,r,c){return e(),i("div",null,[...a[0]||(a[0]=[t(`<h1 id="架构决策-稳定版公开-api-自有薄网关" tabindex="-1">架构决策：稳定版公开 API + 自有薄网关 <a class="header-anchor" href="#架构决策-稳定版公开-api-自有薄网关" aria-label="Permalink to &quot;架构决策：稳定版公开 API + 自有薄网关&quot;">​</a></h1><h2 id="结论" tabindex="-1">结论 <a class="header-anchor" href="#结论" aria-label="Permalink to &quot;结论&quot;">​</a></h2><p>Gateway Server 直接依赖精确锁定的 <code>notebooklm-py==0.7.3</code>，仅调用它的公开 Python API；仓库不再保留 <code>gateway_server/notebooklm/</code> 副本，也不复制参考项目的私有 Server 实现。</p><p>它把 NotebookLM 协议适配交给上游 SDK，把多租户、认证、基础数据存储、任务持久化和前端契约留在本项目。</p><h2 id="为什么不保留内嵌源码" tabindex="-1">为什么不保留内嵌源码 <a class="header-anchor" href="#为什么不保留内嵌源码" aria-label="Permalink to &quot;为什么不保留内嵌源码&quot;">​</a></h2><p>完整 SDK、CLI、MCP、Server 和内部传输层远超本网关需要。仓库只通过 <code>pyproject.toml</code> 和 <code>requirements.lock</code> 声明依赖，业务代码不会依赖 <code>_auth</code>、<code>_artifact</code>、<code>server</code> 等私有模块。SDK 版本变化可以通过公开签名和自动测试评估。</p><h2 id="对参考项目的判断" tabindex="-1">对参考项目的判断 <a class="header-anchor" href="#对参考项目的判断" aria-label="Permalink to &quot;对参考项目的判断&quot;">​</a></h2><p><code>gnh1201/notebooklm-rest-api</code> 证明了“REST 服务直接引用 <code>notebooklm-py</code> 包”的方向可行。本项目独立实现认证挂载、接口覆盖和多租户数据管理。</p><p>本项目借鉴的是依赖方式，不复制其版本、认证或路由实现。</p><h2 id="组件职责" tabindex="-1">组件职责 <a class="header-anchor" href="#组件职责" aria-label="Permalink to &quot;组件职责&quot;">​</a></h2><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TB</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  subgraph Clients[&quot;客户端&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Web[&quot;NoteWeb&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    External[&quot;REST 客户端&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Desktop[&quot;桌面凭据助手&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Console[&quot;管理控制台&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  subgraph Gateway[&quot;自有薄网关&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Auth[&quot;管理/用户 Token 隔离&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Routes[&quot;稳定 REST 契约与校验&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Manager[&quot;有界 ClientManager&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Jobs[&quot;生成任务来源与状态&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Store[&quot;基础 SQLite&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  SDK[&quot;notebooklm-py 0.7.3 公开 API&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Upstream[&quot;NotebookLM&quot;]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Web --&gt; Auth</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  External --&gt; Auth</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Desktop --&gt; Auth</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Console --&gt; Auth</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Auth --&gt; Routes</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Routes --&gt; Manager</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Routes --&gt; Jobs</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Jobs --&gt; Store</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Manager --&gt; Store</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Manager --&gt; SDK</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  SDK --&gt; Upstream</span></span></code></pre></div><h3 id="网关拥有" tabindex="-1">网关拥有 <a class="header-anchor" href="#网关拥有" aria-label="Permalink to &quot;网关拥有&quot;">​</a></h3><ul><li>管理 Token 与用户 API Key 的权限隔离。</li><li>账户状态、密钥唯一性、上传限制和 CORS 策略。</li><li>账户、用户 API Key、Cookie Storage State 和生成任务的基础 SQLite 存储。</li><li>SDK 客户端的创建、并发初始化、复用、回收和 Cookie 回写。</li><li>对外 Schema、错误映射和明确的 HTTP 状态码。</li><li>Studio 任务归属、创建参数、重启恢复、精确下载 ID。</li><li>NoteWeb、管理页和桌面登录工作流。</li></ul><p>NoteWeb 的业务逻辑继续使用原生 ES Modules；<code>liquid-glass-react</code> 通过独立 React 入口只增强标记了 <code>data-liquid-glass</code> 的表面。构建产物随静态文件提交并打包，运行时不依赖 Node.js。组件无法完整工作的浏览器仍使用 CSS <code>backdrop-filter</code>/半透明边框回退，且 <code>prefers-reduced-motion</code> 会关闭弹性运动。</p><h3 id="上游-sdk-拥有" tabindex="-1">上游 SDK 拥有 <a class="header-anchor" href="#上游-sdk-拥有" aria-label="Permalink to &quot;上游 SDK 拥有&quot;">​</a></h3><ul><li>NotebookLM RPC 和响应解析。</li><li>Cookie Storage State 登录会话。</li><li>笔记本、来源、对话、研究、笔记、Studio 和共享的实际操作。</li><li>上游重试、速率限制和认证异常类型。</li></ul><h2 id="数据安全" tabindex="-1">数据安全 <a class="header-anchor" href="#数据安全" aria-label="Permalink to &quot;数据安全&quot;">​</a></h2><p>容器内数据目录默认是 <code>/app/data</code>，Compose 将它保存到自动创建的 <code>notebooklm-gateway-data</code> 命名卷：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>data/</span></span>
<span class="line"><span>├── gateway.db              # 账户与生成任务</span></span>
<span class="line"><span>└── profiles/</span></span>
<span class="line"><span>    └── &lt;account_id&gt;/</span></span>
<span class="line"><span>        └── storage_state.json  # SDK 运行副本，0600</span></span></code></pre></div><p>SQLite 中的 <code>email</code>、<code>api_key</code>、<code>storage_state</code>、账户状态和生成任务均使用直接可读字段，不提供字段级加密或 Key 派生索引。这让项目保持为易理解、易替换的框架基础；二次开发者可以把 <code>DatabaseManager</code> 替换为自己的 ORM、外部数据库或密钥管理实现。</p><p>明文存储意味着数据卷必须视为敏感数据：不要通过静态服务器公开。需要生产级静态加密时，应由部署者使用加密磁盘、云盘加密或自定义数据库层完成。</p><h2 id="客户端生命周期" tabindex="-1">客户端生命周期 <a class="header-anchor" href="#客户端生命周期" aria-label="Permalink to &quot;客户端生命周期&quot;">​</a></h2><p>每个活动账户最多对应一个 SDK 客户端。<code>ClientManager</code>：</p><ol><li>从 SQLite 读取 Storage State。</li><li>原子写入账户专属 Profile，权限设为 <code>0700/0600</code>。</li><li>通过 <code>NotebookLMClient.from_storage</code> 创建公开 SDK 客户端。</li><li>使用每账户锁避免并发重复登录，使用引用计数避免回收使用中的客户端。</li><li>按空闲时间和池容量执行 LRU 回收。</li><li>请求后仅在文件摘要变化时把刷新后的 Cookie 回写数据库。</li><li>只有明确的 <code>AuthError</code> 才把账户标记为 <code>expired</code>，网络错误不会误伤账户。</li></ol><h2 id="studio-任务模型" tabindex="-1">Studio 任务模型 <a class="header-anchor" href="#studio-任务模型" aria-label="Permalink to &quot;Studio 任务模型&quot;">​</a></h2><p>创建生成物前，REST Schema 先校验类型和参数；调用成功拿到 <code>task_id</code> 后，网关保存：账户、笔记本、类型、请求参数、状态和错误。</p><p>轮询必须命中同一账户与笔记本的任务记录，防止跨租户枚举。列表把上游已经可见的生成物与本地未完成任务合并。下载明确传入 <code>artifact_id</code>，避免旧实现“取最新一个”的竞态。</p><p>思维导图的公开 SDK 调用同步返回结果，因此网关将它记录为已完成任务。电影视频完成后由上游以视频生成物表示。</p><h2 id="公开-api-边界" tabindex="-1">公开 API 边界 <a class="header-anchor" href="#公开-api-边界" aria-label="Permalink to &quot;公开 API 边界&quot;">​</a></h2><p>允许：</p><ul><li><code>from notebooklm import NotebookLMClient, ...</code></li><li>SDK 文档和顶层导出的类型、枚举、异常。</li></ul><p>禁止：</p><ul><li><code>notebooklm._*</code></li><li><code>notebooklm.server.*</code></li><li>复制上游包文件后直接修改。</li><li>依赖仅存在于 <code>main</code> 分支、尚未进入稳定发行版的签名。</li></ul><p>因此，稳定版没有公开能力时，网关会明确降级而不绕过边界。例如研究取消返回 <code>501</code>，master-token 自动引导不再提供。</p><h2 id="sdk-升级流程" tabindex="-1">SDK 升级流程 <a class="header-anchor" href="#sdk-升级流程" aria-label="Permalink to &quot;SDK 升级流程&quot;">​</a></h2><ol><li>阅读目标稳定版本的 Python API 文档和 changelog。</li><li>在隔离虚拟环境安装目标版本，核对顶层导出和生成器签名。</li><li>修改 <code>pyproject.toml</code> 的精确版本，并重新生成 <code>requirements.lock</code>。</li><li>调整 Schema、序列化和 NoteWeb 参数；不得用私有模块补齐差异。</li><li>运行单元测试、JavaScript 检查、文档构建、容器构建和浏览器桌面/窄屏验证。</li><li>更新 README、API 和部署说明后发布。</li></ol><p>若稳定版公开 API 无法满足不可妥协的需求，应先写新的架构决策，再考虑受控适配层；不能悄悄恢复整个上游源码副本。</p>`,37)])])}const E=s(n,[["render",l]]);export{k as __pageData,E as default};
